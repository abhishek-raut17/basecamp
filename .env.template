# This is a template of the `.env` file required at the root of the project.
# Copy this file to `.env` and fill values before running the make targets.
# Keep `.env` out of version control when it contains secrets.

# Logging and dry run flag
LOG_LEVEL=0
DRY_RUN=1

# Shared directories
BIN_DIR=path

# Project configs
# PROJECT_NAME: Logical name used to prefix resources (example: basecamp)
PROJECT_NAME=string
# RELEASE_VERSION: Release or deployment version (semver, example: v1.0.0)
RELEASE_VERSION=string

# CLI/tools download URLs (used by bootstrap scripts)
# TALOSCONFIG, KUBECONFIG: local paths where configs will be created
TALOSCONFIG=path
KUBECONFIG=path

# TALOSCTL_URL, KUBECTL_URL, HELM_URL, FLUXCD_URL: download locations for tools
TALOSCTL_URL=url
KUBECTL_URL=url
HELM_URL=url
FLUXCD_URL=url

# --- Terraform / Infrastructure variables ---
# These environment variables are used to populate `infrastructure/terraform.tfvars.template`.
# They replace placeholders in the template and will be written into `terraform.tfvars`

# PROJECT_NAME: string (example: basecamp)
# CLOUD_PROVIDER_REGION: string (example: us-ord)
CLOUD_PROVIDER_REGION=string

# Personal Access Token/SSH keys
# CLOUD_PROVIDER_PAT: sensitive string (Linode API token)
CLOUD_PROVIDER_PAT=string
# GITHUB_PAT: sensitive string (GitHub personal access token)
GITHUB_PAT=string
GIT_REPO=string
# ACCESS_SSHKEY_PATH: path to public ssh key (example: ~/.ssh/basecamp_rsa.pub)
ACCESS_SSHKEY_PATH=path
# FLUXCD_SSHKEY_PATH: path to public ssh key for fluxcd
FLUXCD_SSHKEY_PATH=path

# VPC_CIDR: CIDR for private VPC network (use /16, example: 10.0.0.0/16)
VPC_CIDR=string

# NODETYPE_BASTION, NODETYPE_CLUSTER: instance SKUs for bastion and cluster nodes
NODETYPE_BASTION=string
NODETYPE_CLUSTER=string

# IMG_BASTION, IMG_CLUSTER: image labels or private image IDs
IMG_BASTION=string
IMG_CLUSTER=string

# VERSION_TALOSCTL, VERSION_KUBECTL: tooling versions used during provisioning
VERSION_TALOSCTL=string
VERSION_KUBECTL=string

# WORKER_NODES: integer, number of worker nodes to provision (recommended minimum: 3)
WORKER_NODES=integer

# Terraform Plan output
PLAN=string
