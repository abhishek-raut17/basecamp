apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: external-gateway
  namespace: ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-dev
spec:
  gatewayClassName: traefik
  listeners:
  - name: http
    protocol: HTTP
    port: 80
    allowedRoutes:
      namespaces:
        from: All

  - name: https-dashboard
    protocol: HTTPS
    port: 443
    hostname: "dashboard.sigdep.cloud"
    tls:
      mode: Terminate
      certificateRefs:
      - kind: Secret
        name: dashboard-gateway-tls
        namespace: ingress
    allowedRoutes:
      namespaces:
        from: All
