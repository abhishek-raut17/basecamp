apiVersion: v1
kind: Secret
metadata:
  name: nextcloud-admin-secret
  namespace: nextcloud
type: kubernetes.io/basic-auth
stringData:
  username: nextcloud-admin
  password: "sigdep_nextcloud@admin_user001_v0.1"
  smtpUsername: nextcloud-smtp
  smtpPassword: "sigdep_nextcloud@smtpAdmin_user001_v0.1"
  smtpHost: "smtp-host-key"
